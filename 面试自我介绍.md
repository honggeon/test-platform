# 面试自我介绍 — AI 测试智能体系统平台

> 基于实际项目 `ai-test-agent-system-platform` 生成
> 技术栈：deepagents (LangGraph) + DeepSeek + FastAPI + Next.js 14 + PostgreSQL + MinIO + Playwright

---

## 一、30 秒电梯演讲（极简版）

面试官您好，我是一名资深测试开发工程师。最近主导设计并开发了一个 **AI 驱动的智能测试管理平台**，核心思路是用大模型 + Agent 架构把传统的手工测试流程自动化。

传统 API 测试，测试人员需要手动阅读接口文档、设计用例、写脚本，一个中等复杂度的接口至少需要 2 到 3 天。我们基于 **deepagents + DeepSeek** 构建了一个 AI Agent，用户只需要用自然语言描述测试需求，Agent 自动完成端点解析 → 生成测试计划 → 生成测试代码 → 执行测试 → 甚至自动修复失败的用例。

单接口测试效率提升到 15 到 30 分钟，批量场景下 10 个接口并行处理，效率提升 20 倍以上。

---

## 二、1-2 分钟标准版（适合面试开场）

面试官你好，我从事测试开发工作多年，最近主导了一个 **AI 智能测试系统平台** 的完整设计与开发。

**背景与痛点**

传统的 API 测试流程非常繁琐——测试人员需要去读接口文档、手动设计测试用例、写测试脚本、维护测试数据，一条中等复杂度的接口走完全流程要 2 到 3 天。

**我的任务**

打造一个 AI 驱动的测试平台，让测试人员用自然语言描述需求，AI 来自动搞定解析接口、制定测试计划、生成代码、执行测试、修复失败这一整条链路。

**系统架构**

整个架构分为三层：

- **AI Agent 层**：基于 deepagents + DeepSeek 构建，底层是 LangGraph Pregel 运行时。定义了 4 种核心工作流——单端点测试、批量测试、场景编排测试、失败自动修复。系统内置了 **31 个原子工具**（覆盖端点查询、脚本生成、测试执行、环境配置、场景编排等），还有一个 Skills 知识库体系，按需加载测试计划、代码生成、执行、修复等技能，比全量加载节约 40-60% token。

- **后端服务层**：FastAPI + PostgreSQL（关系数据）+ MinIO（测试脚本和报告附件）。代码架构采用 Service/Repository 分层设计，数据库模型有 22 张表。

- **前端交互层**：Next.js 14 + @langchain/langgraph-sdk，通过 SSE 流式传输实时展示 Agent 的思考过程。核心交互流程是：用户提需求 → Agent 逐步执行并实时回显 → 生成完整测试成果物。

**量化成果**

单条接口的测试从 2-3 天缩短到 15-30 分钟，批量场景下效率提升 20 倍。这个项目从技术选型到代码落地，从 Agent 系统提示词设计到数据库表结构，全部由我一个人主导完成。

---

## 三、3-5 分钟详细版（适合面试深挖）

面试官您好，我叫 xxx，做测试开发有 x 年经验。今天想重点介绍一个我最近独立主导的项目——**AI 测试智能体系统平台**。

### 背景与痛点

我们团队之前做 API 测试，流程是这样的：拿到接口文档 → 人工分析功能点 → 设计等价类/边界值用例 → 用 Playwright 或 Postman 写脚本 → 配置测试数据 → 执行 → 排查失败 → 修脚本。一条中等复杂度的接口，比如登录鉴权、增删改查操作，至少需要 2 到 3 天。代码覆盖率低，人员复用困难。

### 我的任务

我的目标是从零搭建一个 AI 驱动的测试平台，要做到：测试人员只说需求不说怎么写，AI 自动完成解析接口、生成计划、生成用例、生成脚本、执行测试、修复失败的完整闭环。

### 技术选型

使用了 **deepagents** 作为 Agent 框架，底层是 LangGraph Pregel 运行时。选这个组合的原因有三点：

1. **Pregel 图计算模型**：测试流程天然是一个有向无环图（DAG）——获取端点信息 → 生成计划 → 生成用例 → 生成代码 → 执行 → 报告。deepagents 封装了 StateGraph 的构建、中间件编排和 FilesystemBackend，同时保持了 LangGraph Pregel 的兼容性，可以用 LangGraph CLI 部署为 API 服务。

2. **SkillsMiddleware 按需加载**：这是 deepagents 提供的关键能力——6 个 Skills 文件（planner/generator/executor/healer/scenario/reporter）在需要时才注入到 system prompt 中，而不是一次性加载全部。这种方式比全量加载节约 40-60% 的 token，而且知识隔离避免了不同 Skill 之间的冲突。

3. **SSE 流式传输 + Human-in-the-loop**：前端通过 @langchain/langgraph-sdk 的 useStream hook 发起 SSE 连接，Agent 的每一步思考过程实时推送到前端渲染。结合 LangGraph 的 checkpoint 机制和 interrupt_before，可以在关键步骤暂停等待人工审批。

### 架构设计与核心实现

Agent 层分为三层架构：

```
Layer 1: Agent（编排层）—— deepagents.create_deep_agent() → Pregel 实例
Layer 2: Skills（知识层）—— 按需加载的 Skills 文件系统（planner/generator/executor/healer/scenario/reporter）
Layer 3: Tools（执行层）—— 31 个原子工具（MCP 已导入但未接入）
```

工具覆盖八大类：

| 类别 | 数量 | 示例 |
|------|------|------|
| 端点操作 | 4 | list_api_endpoints, get_endpoint_details, get_folder_structure |
| 成果物保存 | 5 | save_test_plan, save_test_cases, save_test_script |
| 测试执行 | 3 | run_tests, run_test_suite, parse_test_results |
| 脚本管理 | 3 | download_api_script, delete_api_script |
| 脚本执行 | 2 | execute_api_script, get_test_execution_status |
| 批量操作 | 2 | batch_generate_tests, batch_run_tests |
| 场景编排 | 10 | create_test_scenario, add_data_mapping, update_scenario_step, add_step_assertion |
| 环境管理 | 2 | list_environments, get_default_environment |

每个工具都遵循统一的 JSON 字符串返回模式：

```python
return json.dumps({"success": True, "data": ..., "error": None})
```

选择 JSON 字符串而非字典的原因：纯文本 LLM 对字符串处理更可靠，统一的错误格式保证了 Agent 不会因为工具执行异常而中断运行。

前后端通信方面，前端通过 `@langchain/langgraph-sdk` 的 `useStream` hook 发起 SSE 连接，每个 Agent 步骤都实时推送到前端渲染。Thread ID 同步到 URL 参数，支持页面刷新后恢复对话上下文。

数据库用了两种存储：PostgreSQL 存结构化业务数据（22 张表，18+ SQLAlchemy 模型，包括端点管理、测试计划、用例、脚本、场景、环境配置等）、MinIO 存测试脚本和报告附件。

### 一个印象深刻的困难

LangGraph SDK 的 `useStream` 在构建 URL 时用的是 `new URL(apiUrl + path)`，当 `.env.local` 配置的是相对路径 `/lg` 时，浏览器端 `new URL` 会报错。我通过封装一个 `resolveApiUrl` 函数，检测到相对路径时拼接 `window.location.origin`，解决了这个跨端兼容问题。

### 量化成果

- 单条接口测试从 2-3 天压缩到 15-30 分钟
- 批量场景下 10 个接口并行处理，效率提升 20 倍
- 31 个原子工具，22 张数据库表
- 全部由我独立完成：技术方案、代码编写、Agent 提示词设计、数据库建模

---

## 四、针对不同岗位的侧重点建议

| 目标岗位 | 重点突出 |
|----------|----------|
| 测试开发（大厂） | Playwright 集成、Allure 报告、测试流程闭环、CI 集成 |
| AI/智能体开发 | deepagents Agent 架构、Skills 知识库设计、工具系统、SSE 流式通信 |
| 后端开发 | FastAPI + Service/Repository 分层、多数据库架构、异步 SQLAlchemy |
| 全栈开发 | Next.js + LangGraph SDK 集成、SSE 流式渲染、国际化（中/英/日） |
| 架构师 | 系统分层设计、Agent 三层架构、deepagents + LangGraph 组合方案 |

---

## 五、面试追问准备清单

根据项目内容，面试官可能追问以下问题，建议提前准备：

**Q1: 为什么选 deepagents + LangGraph 而不是 Coze/Dify 这种低代码平台？**

> 低代码平台适合标准化通用场景。我们的场景有特殊性：深度集成自定义数据库（PG + MinIO）、测试工具有大量领域特定 I/O（读取 OpenAPI、写入 MinIO、执行本地测试脚本）、需要精确控制 prompt 注入顺序。这些在低代码平台要么不支持，要么扩展成本很高。deepagents + LangGraph 给了我们最大的灵活性，同时 deepagents 的 SkillsMiddleware 和 FilesystemBackend 又让我们不必手写 LangGraph 的底层样板代码。

**Q2: deepagents 和 LangGraph 的关系是什么？**

> deepagents 是一个封装层，提供了 `create_deep_agent()` 工厂函数，内部帮你做 StateGraph 的构建、中间件编排、FilesystemBackend 初始化。但它生成的仍然是标准的 LangGraph Pregel 实例。这意味着你可以用 LangGraph CLI 来托管和部署它，前端也可以用 @langchain/langgraph-sdk 来消费它。所以 deepagents 是更上层的抽象，底层还是 LangGraph。

**Q3: Agent 的工具为什么会返回 JSON 字符串而不直接返回字典？**

> 纯文本 LLM 对字符串处理更可靠。如果返回字典，LangGraph 序列化后再传给 LLM，中间可能丢失格式信息。我们主动控制序列化，并且统一 `{"success": true/false, "error": ...}` 格式，Agent 在任何工具执行异常时都能继续运行，不会中断。

**Q4: SSE 和 WebSocket 的差异？为什么选 SSE？**

> SSE 是单向通道（服务端 → 客户端），对于 Agent 流式输出已经够了。WebSocket 是双向的，但需要单独处理心跳、重连。SSE 天然基于 HTTP，防火墙友好，浏览器原生支持 EventSource，Next.js 的 rewrite 也能透明代理。

**Q5: Human-in-the-loop 是怎么实现的？**

> LangGraph 的 `interruptBefore: ["tools"]` 机制。Agent 在调用工具前暂停执行，将待审批的工具调用列表返回给前端。用户通过审批组件审查并点击确认后，前端调用 `stream.resume()` 继续执行流程。deepagents 在这里没有额外封装，直接透传了 LangGraph 的 checkpoint 能力。

**Q6: 如果让你设计多租户 SaaS 版本，你会怎么做？**

> 1. PostgreSQL 加 `tenant_id` 列 + Row-Level Security 行级隔离
> 2. JWT 中解析 tenant_id，在中间件中注入
> 3. Agent 上下文中注入 tenant_id，确保工具操作只访问当前租户的数据
> 4. MinIO 使用前缀隔离：`tenant_a/api_tests/...` / `tenant_b/api_tests/...`
> 5. 限流改用 Redis，按 tenant 级别控制并发

**Q7: SkillsMiddleware 按需加载是怎么实现的？**

> SkillsMiddleware 是一个 AgentMiddleware，拦截每次 LLM 调用。它检查当前任务是否需要加载某个 Skill，如果需要就从 FilesystemBackend 读取对应文件（如 planner.md、generator.md），动态注入到 system prompt 中。这样 6 个 Knowledge Skill 不需要同时加载，只有在触发对应阶段时才注入，节约 40-60% token。而且不同的 Skill 之间知识隔离，不会互相冲突。

---

## 六、量化数据速查表

| 指标 | 具体数字 |
|------|----------|
| 前后端耗时 | 单接口 2-3天 → 15-30分钟 |
| 批量效率提升 | 20倍+ |
| 原子工具 | 31 个 local @tool |
| 数据库表 | 22 张 PostgreSQL 表 |
| Agent 工作流 | 4 种（A: 单端点 / B: 修复 / C: 批量 / D: 场景） |
| Skills 知识库 | 6 个（planner/generator/executor/healer/scenario/reporter） |
| Agent 框架 | deepagents（底层 LangGraph Pregel）|
| 前端国际化 | 中/英/日三语 |
| UI 前端框架 | Next.js 14 + Tailwind + Radix UI |
| SSE 通信 | @langchain/langgraph-sdk useStream，实时流式，支持中断恢复 |
|| 场景测试能力 | JSONPath 数据提取、数据映射、断言链、步骤更新 |
