# 周报 — AI 测试智能体系统平台

**汇报人**: 洪革  
**日期**: 2026/06/01 — 2026/06/05  
**项目**: AI 测试智能体系统平台

---

## 一、本周工作概要

本周完成 AI 测试智能体系统平台的整体收尾、部署上线与问题修复。系统已实现覆盖软件测试全生命周期的智能化管理，后端 200+ Python 模块、前端 12 个功能页面，支持测试用例管理、API/Web/场景自动化测试、AI 智能诊断、代码知识图谱分析等核心能力，本周正式交付上线。

---

## 二、重点工作项

### 1. 智能诊断系统前端集成 + 后端诊断引擎（新功能）

**技术栈**: FastAPI + LangGraph + WebSocket + Redis Pub/Sub + PostgreSQL + MongoDB

- 完成了基于规则引擎 + LLM 的测试失败自动诊断系统
- 实现 6 阶段诊断流水线（日志采集 → 规则匹配 → 知识图谱查询 → LLM 分析 → 报告生成 → 通知推送）
- 诊断报告采用 PostgreSQL + MongoDB 双写架构，PG 用于列表查询，MongoDB 存储完整诊断文档
- 实现 WebSocket 实时推送诊断进度，支持 Redis Pub/Sub 多实例广播
- 完善降级设计：DB 超时、KG 超时、LLM 超时均有分级降级处理（none/partial/severe）
- 前端集成诊断页面，包含诊断进度展示、报告详情、WebSocket 实时通知弹窗
- 后端核心服务 894 行，配套 402 行 pytest 测试用例

### 2. 全栈代码分析模块（新功能）

**技术栈**: Python 知识图谱引擎 + React + D3 可视化

- 完成代码知识图谱引擎：支持 Git 仓库和本地路径两种源码输入方式
- 实现 15 个分析阶段管道（文件扫描 → 符号提取 → 导入分析 → 调用链 → 路由提取 → ORM 提取 → 社区发现 → 执行流等）
- 支持多版本图谱持久化到 PostgreSQL，带 commit 追溯和增量更新
- 前端实现图谱探索（D3 可视化）、符号搜索（hybrid 混合检索）、变更影响分析、执行流追踪四大功能
- 集成 AI 对话面板，支持基于图谱的智能代码问答

### 3. 场景测试编排引擎（新功能）

**技术栈**: Python + pytest + Allure

- 完成多接口业务流程编排引擎（995 行核心代码）
- 支持步骤串联、变量映射、数据驱动、断言提取
- 实现场景在前端可视化管理（场景列表、步骤编辑、变量绑定）
- 集成 Allure 报告生成，支持场景执行结果可视化
- 后端支持 HAT 框架驱动 YAML/Excel 用例执行

### 4. API 自动化测试模块（功能增强）

- 完善 OpenAPI/Swagger 文档解析与端点自动识别
- 实现 AI 驱动的测试计划/脚本/用例生成（集成 DeepSeek）
- 优化 API 端点侧边栏、测试脚本列表、执行日志面板等前端组件
- 工具注册子系统重构：拆分 batch、environment、script、test_artifacts、test_execution 等独立工具模块

### 5. 部署环境搭建与配置（运维）

- 远程服务器（192.168.0.182）部署 MinIO 对象存储（docker-compose）
- 解决端口冲突问题（已有 xuanjian-minio 占用 9002/9003，新实例分配 9004/9005）
- 配置 PostgreSQL 数据库连接、MongoDB 文档存储
- 统一环境变量管理：整合项目根目录、backend/ 多级 .env 配置
- 修复 MinIO 连接拒绝问题（端点从旧 localhost:9002 迁移至新实例 192.168.0.182:9004）

### 6. 前端 UI 优化与问题修复（迭代改进）

- 全栈分析页面：修复"分析"按钮未自动保存仓库配置导致的 400 错误，改为点击分析时先保存再触发
- 前端 API 客户端：修复 FastAPI HTTPException 错误消息提取逻辑，兼容 `detail` 和 `message` 两种字段格式
- 多个页面的 UI 细节优化、组件重构
- TypeScript 类型完善、i18n 文案更新
- 清理废弃文档和代码文件

---

## 三、系统架构总览

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | Next.js 14 + React 18 + Tailwind CSS + shadcn/ui | App Router, 12 个功能页面 |
| API 网关 | FastAPI + 自定义中间件 | 速率限制、异常处理、JWT 认证 |
| AI 服务 | LangGraph + DeepAgents | 3 个 Agent（API 测试、代码分析、日志诊断）|
| 关系数据库 | PostgreSQL + SQLAlchemy 2.0 Async | 22+ 张业务表 |
| 文档数据库 | MongoDB + Motor | 诊断报告、API 日志、审计记录 |
| 对象存储 | MinIO (S3 兼容) | 脚本、附件、报告文件 |
| 缓存消息 | Redis | 诊断 WebSocket Pub/Sub |

**三服务运行时**: FastAPI(:8000) + LangGraph Agent(:2026) + Next.js(:3000)

---

## 四、关键数据

| 指标 | 数值 |
|------|------|
| 后端 Python 模块 | 200+ 文件 |
| 前端 TypeScript 组件 | 12 个功能页面，13 个组件目录 |
| AI Agent | 3 个（api_agent / code_analysis_agent / log_analysis_agent）|
| API 路由模块 | 24 个 v2 端点模块 |
| 业务服务 | 29 个 Service |
| 数据仓库 | 16 个 Repository |
| 知识图谱引擎 | 33 个模块，15 个分析阶段 |
| 后端测试 | 14 个测试文件 |
| i18n | 中文 / English / 日本語 |

---

## 五、下周计划

1. 编写系统用户操作手册
2. 补充前端单元测试和 E2E 测试
3. 完善 CI/CD 自动化部署流水线
4. 容器化改造（Dockerfile + docker-compose 全栈部署）
5. 性能压测与优化（特别是诊断引擎和知识图谱分析的大代码库场景）
6. 安全审计：API Key 管理、敏感数据脱敏覆盖、JWT 过期策略

---

## 六、风险与问题

- **MinIO 存储可靠性**: 当前单实例部署，后续需考虑数据备份和高可用方案
- **无容器化**: 目前依赖 nohup + PID 文件管理进程，缺少健康检查和自动重启机制
- **前端测试缺失**: 当前无 Jest/Vitest/Playwright 测试，回归依赖人工验证
- **知识图谱性能**: 大型代码库（10 万+ 文件）的分析性能待验证
